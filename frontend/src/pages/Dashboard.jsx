import { Typography, Box } from '@mui/material';

export default function Dashboard() { // <--- Change function name for each file
  return (
    <Box>
      <Typography variant="h4">Dashboard Page</Typography> {/* <--- Change text */}
    </Box>
  );
}